import React from "react";
import classes from "./Header.module.css";

const Header = () => {
  return (
    <header className={classes.header}>ТУТ БУДУТ ВСЯКИЕ ИКОНКИ И КНОПКИ</header>
  );
};

export default Header;
